<Window x:Class="OperatorOfAAirport.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OperatorOfAAirport"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        mc:Ignorable="d"
        Title="LogIn" Height="400" Width="300" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" WindowStyle="None">

    <Grid>
        <materialDesign:Flipper HorizontalAlignment="Center" VerticalAlignment="Center" x:Name="d">
            <materialDesign:Flipper.FrontContent>
                <StackPanel >
                    <TextBox Width="180" x:Name="LoginTextBox" materialDesign:HintAssist.Hint="Login" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="15,150,15,10" ></TextBox>

                    <PasswordBox Width="180" x:Name="PasswordBox" materialDesign:HintAssist.Hint="Password" Margin="15" />

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 0 0 20">
                        <Button Margin="15 30" Content="Log in" Click="Button_Click_Login"/>
                        <Button Margin="15 30" Content="Sign up" Command="{x:Static materialDesign:Flipper.FlipCommand}"/>
                    </StackPanel>
                    <Button Command="{x:Static materialDesign:DialogHost.OpenDialogCommand}" CommandTarget="{Binding ElementName=DialogWindowExit}" Style="{StaticResource MaterialDesignFlatButton}">EXIT</Button>

                </StackPanel>
            </materialDesign:Flipper.FrontContent>

            <materialDesign:Flipper.BackContent>
                <StackPanel>

                    <TextBox Width="180" materialDesign:HintAssist.Hint="Your First Name"  Margin="15,115,15,0"/>
                    <TextBox Width="180" materialDesign:HintAssist.Hint="Your Last Name"  Margin="15,20,15,0"/>
                    <TextBox Width="180" materialDesign:HintAssist.Hint="Your Middle Name"  Margin="15,20,15,0"/>
                    <TextBox Width="180" materialDesign:HintAssist.Hint="Login" Margin="15,20,15,0"/>
                    <PasswordBox Width="180" materialDesign:HintAssist.Hint="Password"  Margin="15,20,15,0"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 0 0 10">
                        <Button Margin="15 30" Content="Back" Command="{x:Static materialDesign:Flipper.FlipCommand}" Style="{StaticResource MaterialDesignFlatButton}"/>
                        <Button Margin="15 30" Content="Sing up" Command="{x:Static materialDesign:Flipper.FlipCommand}"/>
                    </StackPanel>
                </StackPanel>
            </materialDesign:Flipper.BackContent>

        </materialDesign:Flipper>
        
<!--/////////////////////////////////////////////////Диалоговое окно//////////////////////////////////////-->
        <materialDesign:DialogHost IsOpen="False" x:Name="DialogWindowExit">
            <materialDesign:DialogHost.DialogContent>
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20">
                    <TextBlock HorizontalAlignment="Center">Are you sure?</TextBlock>
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <Button Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}" CommandTarget="{Binding ElementName=DialogWindowExit}" Style="{StaticResource MaterialDesignFlatButton}" Content="NO"></Button>
                        <Button Style="{StaticResource MaterialDesignFlatButton}" Content="YES" Click="Click_ExitButton"></Button>
                    </StackPanel>
                </StackPanel>
            </materialDesign:DialogHost.DialogContent>
        </materialDesign:DialogHost>

    </Grid>
</Window>
